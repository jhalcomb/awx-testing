---
- name: PLAY 1 - PWD
  hosts: localhost
  tasks:

    - name: TASK 1 - Run 'pwd'
      ansible.builtin.debug:
        commands:
          - pwd
      register: dir

    - name: TASK 2 - Display 'pwd'
      ansible.builtin.debug:
        msg: "{{ dir }}"